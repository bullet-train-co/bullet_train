<% if object.send(attribute).present? %>
  <%= render 'shared/attributes/attribute', object: object, attribute: attribute do %>
    <% # `.to_s` is for action text. %>
    <%= object.send(attribute).to_s.html_safe %>
  <% end %>
<% end %>

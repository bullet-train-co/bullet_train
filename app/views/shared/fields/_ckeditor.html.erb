<% yield %>

<%
form ||= current_fields_form
options ||= {}
options[:class] = [options[:class], 'ckeditor'].compact.join(' ')
other_options ||= {}
%>

<%= render 'shared/fields/field', form: form, method: method, helper: :text_area, options: options, other_options: other_options %>

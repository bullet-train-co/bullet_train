<%= render 'shared/menu/item', {
  url: main_app.polymorphic_path([:admin, Application.first]),
  label: t('menus.main.labels.admin'),
} do |p| %>
  <% p.content_for :icon do %>
    <i class="fal fa-lock ti ti-lock"></i>
  <% end %>
<% end %>

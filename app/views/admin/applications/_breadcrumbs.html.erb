<% application ||= @application %>
<%= render 'shared/breadcrumbs/actions', only_for: 'applications', first: true %>
<% if (action_name == 'show' && controller_name == 'applications') || current_user.one_application? %>
  <%= render 'shared/breadcrumb', label: 'Dashboard', url: [:admin, application] %>
<% end %>

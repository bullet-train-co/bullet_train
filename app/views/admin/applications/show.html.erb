<%= render 'shared/page' do |p| %>
  <% p.content_for :title, t('.header', applications_possessive: possessive_string(@application.name)) %>

  <% p.content_for :actions do %>
    <%= link_to edit_admin_application_path(@application), class: 'button-secondary button-smaller' do %>
      <%= t('applications.buttons.edit') %>
    <% end if can? :edit, @application %>
  <% end %>

  <% p.content_for :body do %>
    <%# 🚅 super scaffolding will insert new children above this line. %>
  <% end %>
<% end %>

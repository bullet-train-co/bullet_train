<% if can? :read, Team.new %>
  <%= render 'shared/menu/item', {
    url: main_app.polymorphic_path([:admin, Application.first, :teams]),
    label: t('teams.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('teams.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>

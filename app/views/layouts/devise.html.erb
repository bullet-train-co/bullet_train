<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/head' %>
  </head>
  <body class="bg-light-blue-gradient text-gray-700 text-sm font-normal">
    <div data-turbo="false">
      <%= yield %>
    </div>
  </body>
</html>

<%= render 'account/shared/menu/item', {
  url: main_app.polymorphic_url([:account, current_team]),
  label: t('menus.main.labels.dashboard'),
} do |p| %>
  <% p.content_for :icon do %>
    <i class="fal fa-home-lg-alt"></i>
  <% end %>
<% end %>

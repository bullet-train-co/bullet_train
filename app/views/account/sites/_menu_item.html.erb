<% if can? :read, Site.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :sites]),
    label: t('sites.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('sites.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>

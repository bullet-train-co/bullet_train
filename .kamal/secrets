# The easiest way to get started here is to set your secrets as environment
# variables in your bashrc/zshrc and add them here as values:
#
# KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
# RAILS_MASTER_KEY=$(cat config/credentials/production.key)
# UNTITLED_APPLICATION_DATABASE_PASSWORD=$UNTITLED_APPLICATION_DATABASE_PASSWORD
# POSTGRES_PASSWORD=$POSTGRES_PASSWORD
# REDIS_PASSWORD=$REDIS_PASSWORD
# POSTMARK_API_TOKEN=$POSTMARK_API_TOKEN
#
# The recomended way is to use 1Password, LastPass, or Bitwarden to store your
# secrets, like this:
#
# SECRETS=$(kamal secrets fetch --adapter 1password --account ,,,, --from MyApp/KamalSecrets KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY UNTITLED_APPLICATION_DATABASE_PASSWORD POSTGRES_PASSWORD SECRET_KEY_BASE REDIS_PASSWORD POSTMARK_API_TOKEN)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD $SECRETS)
# RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY $SECRETS)
# UNTITLED_APPLICATION_DATABASE_PASSWORD=$(kamal secrets extract UNTITLED_APPLICATION_DATABASE_PASSWORD $SECRETS)
# POSTGRES_PASSWORD=$(kamal secrets extract POSTGRES_PASSWORD $SECRETS)
# SECRET_KEY_BASE=$(kamal secrets extract SECRET_KEY_BASE $SECRETS)
# REDIS_PASSWORD=$(kamal secrets extract REDIS_PASSWORD $SECRETS)
# POSTMARK_API_TOKEN=$(kamal secrets extract POSTMARK_API_TOKEN $SECRETS)

# FIXME: Pick your strategy here:
# INFO: With the current BT bin/configure setup, you need both vars with the same Postgres password.
KAMAL_REGISTRY_PASSWORD=
RAILS_MASTER_KEY=
UNTITLED_APPLICATION_DATABASE_PASSWORD=
POSTGRES_PASSWORD=
REDIS_PASSWORD=
POSTMARK_API_TOKEN=

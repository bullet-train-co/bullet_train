#!/usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
APP_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"

docker run -it \
  --mount type=bind,src="${APP_ROOT}",dst=/rails \
  -e DATABASE_USERNAME="$(whoami)" \
  untitled_application_dev \
  bin/rails console

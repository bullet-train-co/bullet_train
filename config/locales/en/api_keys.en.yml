en:
  api_keys: &api_keys
    label: &label API Keys
    breadcrumbs:
      label: *label
    navigation:
      label: *label
      icon: fal fa-key ti ti-key
    notifications:
      created: API Key was successfully created.
      destroyed: API Key was successfully revoked.
    buttons: &buttons
      reveal: Click to Reveal
      create: Generate New API Key
      destroy: Revoke
      confirmations:
        destroy: Are you sure you want to remove this API Key?
    fields: &fields
      token:
        name: &token Public Token
        label: *token
        heading: *token
      secret:
        name: &secret Secret Token
        label: *secret
        heading: *secret
      last_used:
        name: &last_used Last Used
        label: *last_used
        heading: *last_used
      # 🚅 super scaffolding will insert new fields above this line.
      created_at:
        name: &created_at Generated
        heading: *created_at
    index:
      section: '%{users_possessive} API Keys'
      contexts:
        user:
          header: API Keys
          description: It's okay if other people see the public token below, but you should keep the secret token completely secure.
          description_empty: No API Keys have been generated for %{user_name} yet. To get started, use the button below!
      fields: *fields
      buttons: *buttons
  account:
    api_keys: *api_keys
  activerecord:
    attributes:
      api_key:
        token: *token
        secret: *secret
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
        created_at: *created_at
